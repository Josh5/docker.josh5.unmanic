#!/bin/sh

. /etc/profile

# stop and remove container
docker stop unmanic

# remove image from cron updater list
sed -i '/unmanic/d' /storage/.kodi/userdata/addon_data/docker.linuxserver.updater/update.sh
